global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: cloudtask-api
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - backend:8080
        labels:
          service: cloudtask-api
          environment: local
